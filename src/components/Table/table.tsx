import React, { FC, ButtonHTMLAttributes, AnchorHTMLAttributes } from "react";
import classNames from "classnames";
import './style.scss'
export const Button: FC = (props) => {
  return <div className="btn">123</div>;
};

export default Button;
